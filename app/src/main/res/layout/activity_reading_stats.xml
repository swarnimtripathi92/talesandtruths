<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_reading_stats_screen"
    android:fillViewport="true"
    android:padding="18dp">

    <LinearLayout
        android:id="@+id/rootLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="📊 Reading Stats"
            android:textColor="#1F2A44"
            android:textSize="28sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="18dp"
            android:text="Tiny progress, giant reader energy ✨"
            android:textColor="#5F6B87"
            android:textSize="14sp" />

        <include
            android:id="@+id/cardToday"
            layout="@layout/layout_stats_card" />

        <include
            android:id="@+id/cardWeek"
            layout="@layout/layout_stats_card" />

        <include
            android:id="@+id/cardAllTime"
            layout="@layout/layout_stats_card" />

    </LinearLayout>
</ScrollView>
